# 开发实用篇 — 邮件发送 & 消息队列

> 项目源码路径：`src/main/java/com/example/demo/`
> 测试源码路径：`src/test/java/com/example/demo/`
> 资源路径：`src/main/resources/`

---

# 一、SpringBoot 邮件发送

## 1.1 前置准备

### 文件：`pom.xml`

```xml
<!-- SpringBoot 邮件发送 starter，内置 JavaMailSender -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-mail</artifactId>
</dependency>
```

### 文件：`src/main/resources/application.yml`

```yaml
spring:
  mail:
    host: smtp.qq.com                          # QQ邮箱 SMTP 服务器地址
    port: 587                                  # QQ邮箱端口（587 或 465）
    username: 你的QQ邮箱@qq.com                 # 发件人邮箱
    password: 邮箱授权码                        # 授权码！不是QQ登录密码，需在QQ邮箱设置中开启POP3/SMTP后获取
    default-encoding: UTF-8                    # 邮件默认编码
    properties:
      mail:
        smtp:
          starttls:
            enable: true                       # 启用 STARTTLS 加密
            required: true                     # 要求必须 TLS 加密
```

---

## 1.2 简单邮件 — SimpleMailMessage（第119课）

### 核心知识点

| 概念 | 说明 |
|------|------|
| 核心类 | `SimpleMailMessage` — 轻量纯文本邮件 |
| 发送器 | `JavaMailSender` — Spring 自动注入，无需手动创建 |
| 能力 | 纯文本正文，支持抄送 cc、密送 bcc |
| 局限 | **不支持 HTML、附件、内嵌图片** |

### 文件：`src/main/java/com/example/demo/service/MailService.java`

```java
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;

@Service
public class MailService {

    @Autowired
    private JavaMailSender javaMailSender; // Spring 自动注入的邮件发送器

    /**
     * 发送简单纯文本邮件
     * @param to      收件人邮箱
     * @param subject 邮件标题
     * @param content 邮件正文（纯文本）
     */
    public void sendSimpleMail(String to, String subject, String content) {
        SimpleMailMessage message = new SimpleMailMessage();
        message.setFrom("xxx@qq.com");   // 发件人（必须和 yml 配置的 username 一致）
        message.setTo(to);               // 收件人
        message.setSubject(subject);     // 邮件标题
        message.setText(content);        // 邮件正文（纯文本）
        // 可选：message.setCc("xxx@qq.com")   → 抄送
        // 可选：message.setBcc("xxx@qq.com")  → 密送

        javaMailSender.send(message);    // 发送邮件
    }
}
```

### 文件：`src/main/java/com/example/demo/controller/MailController.java`

```java
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MailController {

    @Autowired
    private MailService mailService;

    /**
     * 测试简单邮件发送
     * GET /sendSimple
     */
    @GetMapping("/sendSimple")
    public String testSimpleMail() {
        mailService.sendSimpleMail(
                "接收邮箱@xxx.com",
                "【通知】测试简单邮件",
                "这是一封纯文本简单邮件！"
        );
        return "简单邮件发送成功";
    }
}
```

---

## 1.3 多部件邮件 — MimeMessageHelper（第120课）

### 核心知识点

| 概念 | 说明 |
|------|------|
| `MimeMessage` | 多部件邮件对象，支持文本+附件+图片 |
| `MimeMessageHelper` | 工具类，构造参数 `true` 开启多部件模式（multipart） |
| `helper.setText(html, true)` | 第二个 `true` 表示正文是 HTML 格式 |
| `helper.addAttachment()` | 添加附件 |
| 适用场景 | HTML 富文本邮件、带附件账单/报表、验证码邮件 |

### SimpleMailMessage vs MimeMessageHelper 对比

| 特性 | SimpleMailMessage | MimeMessageHelper(true) |
|------|-------------------|------------------------|
| 文本格式 | 仅纯文本 | HTML 富文本 |
| 附件 | 不支持 | 支持 |
| 内嵌图片 | 不支持 | 支持 |
| 复杂度 | 轻量 | 功能完整 |
| 使用场景 | 简单通知 | 业务邮件（验证码、报表、账单） |

### 文件：`src/main/java/com/example/demo/service/MailService.java`（多部件方法）

```java
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.core.io.FileSystemResource;  // 本地文件资源
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.mail.javamail.MimeMessageHelper;
import org.springframework.stereotype.Service;
import jakarta.mail.MessagingException;                // 邮件异常
import jakarta.mail.internet.MimeMessage;
import java.io.File;

@Service
public class MailService {

    @Autowired
    private JavaMailSender javaMailSender;

    /**
     * 发送多部件邮件：HTML正文 + 附件
     * @param to          收件人
     * @param subject     邮件标题
     * @param htmlContent HTML 格式正文
     * @param file        附件文件
     * @throws MessagingException 邮件构建异常
     */
    public void sendMultiPartMail(String to, String subject, String htmlContent, File file)
            throws MessagingException {

        // 1. 创建 MimeMessage 多部件消息对象
        MimeMessage mimeMessage = javaMailSender.createMimeMessage();

        // 2. 创建 Helper，第二个参数 true = 开启多部件模式（支持附件和内嵌图片）
        MimeMessageHelper helper = new MimeMessageHelper(mimeMessage, true, "UTF-8");

        // 3. 设置邮件基本信息
        helper.setFrom("xxx@qq.com");         // 发件人
        helper.setTo(to);                     // 收件人
        helper.setSubject(subject);           // 标题
        helper.setText(htmlContent, true);    // 第二个 true = 内容是 HTML

        // 4. 添加附件：参数1 = 附件在邮件中显示的文件名，参数2 = 文件资源
        FileSystemResource resource = new FileSystemResource(file);
        helper.addAttachment(file.getName(), resource);

        // 5. 发送
        javaMailSender.send(mimeMessage);
    }
}
```

### 文件：`src/main/java/com/example/demo/controller/MailController.java`（测试接口）

```java
import jakarta.mail.MessagingException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
import java.io.File;

@RestController
public class MailController {

    @Autowired
    private MailService mailService;

    /**
     * 测试多部件邮件发送
     * GET /sendMulti
     */
    @GetMapping("/sendMulti")
    public String testMultiMail() throws MessagingException {
        // HTML 格式正文
        String html = "<h3>您好！</h3><p>这是一封<b>带附件</b>的多部件邮件，请查看下方附件</p>";

        // 本地文件作为附件
        File attachFile = new File("D:/test.pdf");

        mailService.sendMultiPartMail(
                "接收邮箱@xxx.com",
                "【通知】带附件邮件",
                html,
                attachFile
        );
        return "多部件邮件发送成功";
    }
}
```

---

# 二、消息队列基础（第121课）

## 2.1 核心知识点

### 什么是消息队列（MQ）

消息队列是基于**生产者-消费者模型**的异步通信中间件。消息存入队列，消费者异步拉取处理。

### 三大核心角色

| 角色 | 说明 |
|------|------|
| **Producer 生产者** | 产生消息，发送到队列 |
| **Broker 消息服务端** | 消息中间件（RabbitMQ / RocketMQ / Kafka），负责存储和转发 |
| **Consumer 消费者** | 从队列获取消息，执行业务逻辑 |

### 三大核心特性

| 特性 | 说明 |
|------|------|
| **异步解耦** | 生产者不用等消费者处理完，上下游互不依赖 |
| **削峰填谷** | 高并发请求先入队列，消费者匀速消费，保护下游 |
| **最终一致性** | 通过消息重试、事务消息保证数据最终一致 |

### 同步调用 vs 异步消息

| 模式 | 特点 | 缺点 |
|------|------|------|
| **同步调用** | A 调 B，阻塞等结果 | 耦合高，高峰容易雪崩 |
| **异步消息** | A 发消息立刻返回，B 后台处理 | 架构复杂度增加 |

### 主流 MQ 对比

| 中间件 | 特点 | 适用场景 |
|--------|------|---------|
| **RabbitMQ** | 功能完善，交换机灵活，可靠投递 | 业务订单、通知、可靠性要求高 |
| **RocketMQ** | 阿里开源，高可靠、事务消息 | 电商、金融场景 |
| **Kafka** | 超高吞吐，海量日志 | 日志收集、大数据流处理 |

### 核心名词

| 名词 | 说明 |
|------|------|
| Queue 队列 | 存放消息的容器 |
| Topic 主题 | 消息分类（Kafka/RocketMQ 使用） |
| Exchange 交换机 | RabbitMQ 专用，将消息路由到队列 |
| 点对点 | 一条消息只被一个消费者消费 |
| 发布订阅 | 一条消息广播给多个消费者 |

---

## 2.2 RabbitMQ 入门示例（最通用）

### 文件：`pom.xml`

```xml
<!-- spring-boot-starter-amqp：RabbitMQ 官方 SpringBoot starter -->
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>
```

### 文件：`src/main/resources/application.yml`

```yaml
spring:
  rabbitmq:
    host: 127.0.0.1                    # RabbitMQ 服务器地址
    port: 5672                         # RabbitMQ 默认端口（不是管理界面 15672）
    username: guest                    # 默认用户名
    password: guest                    # 默认密码
    virtual-host: /                    # 虚拟主机，默认 /
```

### 文件：`src/main/java/com/example/demo/config/RabbitConfig.java`（队列配置）

```java
import org.springframework.amqp.core.Queue;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class RabbitConfig {

    // 队列名称常量，方便生产者和消费者统一引用
    public static final String TEST_QUEUE = "test_message_queue";

    /**
     * 创建持久化队列 Bean
     * 参数1：队列名称
     * 参数2：true = 持久化（RabbitMQ 重启后队列不丢失）
     */
    @Bean
    public Queue testQueue() {
        return new Queue(TEST_QUEUE, true);
    }
}
```

### 文件：`src/main/java/com/example/demo/controller/MessageProducerController.java`（生产者）

```java
import org.springframework.amqp.rabbit.core.RabbitTemplate;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MessageProducerController {

    @Autowired
    private RabbitTemplate rabbitTemplate; // RabbitMQ 操作模板，Spring 自动注入

    /**
     * 发送消息到队列
     * GET /sendMsg
     */
    @GetMapping("/sendMsg")
    public String sendMessage() {
        String msgContent = "【测试消息】这是一条通过MQ发送的业务消息";

        // convertAndSend：将消息序列化后发送到指定队列
        // 参数1：队列名  参数2：消息内容（自动JSON序列化）
        rabbitTemplate.convertAndSend(RabbitConfig.TEST_QUEUE, msgContent);

        return "消息发送成功：" + msgContent;
    }
}
```

### 文件：`src/main/java/com/example/demo/listener/MessageConsumer.java`（消费者）

```java
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Component;

@Component
public class MessageConsumer {

    /**
     * @RabbitListener：持续监听指定队列，有消息到达时自动调用此方法
     * queues = 队列名，和生产者发往的队列一致
     * 方法参数自动接收消息内容，Spring 自动完成反序列化
     */
    @RabbitListener(queues = RabbitConfig.TEST_QUEUE)
    public void handleMessage(String message) {
        System.out.println("消费者收到消息：" + message);
        // 在这里写业务逻辑：发短信、发邮件、生成报表、数据同步...
    }
}
```

### RabbitMQ 执行流程

```
调用 /sendMsg → 生产者发送消息 → RabbitMQ Broker 存入队列
→ 消费者 @RabbitListener 监听到新消息 → 自动调用 handleMessage() → 处理业务
生产者不需要等待消费者执行完毕，实现了异步通信
```

---

# 三、RocketMQ（第128-129课）

## 3.1 RocketMQ 安装 & 架构（第128课）

### 四大核心组件

| 组件 | 作用 | 默认端口 |
|------|------|---------|
| **NameServer** | 注册中心，保存 Broker 路由信息，无状态，集群之间互不通信 | **9876** |
| **Broker** | 消息核心服务，负责接收、存储、转发消息；分为 Master/Slave | **10911** |
| **Producer** | 生产者，发送消息 | — |
| **Consumer** | 消费者，消费消息 | — |

### 启动顺序（硬性要求）

```
① 先启动 NameServer → ② 再启动 Broker
顺序不能反！Broker 启动时向 NameServer 注册。
```

### 常见踩坑

| 问题 | 排查方向 |
|------|---------|
| 启动失败 | 检查 JDK 版本、端口占用、内存配置(`runbroker.sh`) |
| 连接不上 | NameServer 地址是否正确，Broker 是否注册成功 |
| 发送失败 | 检查 Topic 是否存在，生产者组是否配置 |

---

## 3.2 SpringBoot 整合 RocketMQ（第129课）

### 核心知识点

| 概念 | 说明 |
|------|------|
| `RocketMQTemplate` | 生产者发送消息的核心 API |
| `@RocketMQMessageListener` | 消费者监听 Topic 的注解 |
| **Topic** | 消息主题，消息分类标签 |
| **ConsumerGroup** | 消费者组；同组多消费者**集群模式**分摊消息 |
| **集群消费** | 同组消费者均分消息，一条消息只被一个消费者处理 |
| **广播消费** | 同组每个消费者都收到全部消息 |

### 文件：`pom.xml`

```xml
<!-- RocketMQ SpringBoot Starter -->
<dependency>
    <groupId>org.apache.rocketmq</groupId>
    <artifactId>rocketmq-spring-boot-starter</artifactId>
    <version>2.2.3</version>
</dependency>
```

### 文件：`src/main/resources/application.yml`

```yaml
rocketmq:
  name-server: 127.0.0.1:9876            # NameServer 地址（RocketMQ 的注册中心）
  producer:
    group: demo-producer-group            # 生产者分组名称，自定义
```

### 文件：`src/main/java/com/example/demo/controller/MqProducerController.java`（生产者）

```java
import org.apache.rocketmq.spring.core.RocketMQTemplate;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class MqProducerController {

    @Autowired
    private RocketMQTemplate rocketMQTemplate; // RocketMQ 操作模板，Spring 自动注入

    // Topic 名称常量，生产者和消费者共用
    public static final String TOPIC_NAME = "demo-topic";

    /**
     * 发送消息到指定 Topic
     * GET /sendMsg
     */
    @GetMapping("/sendMsg")
    public String sendMessage() {
        String msg = "Hello RocketMQ SpringBoot整合测试消息";

        // convertAndSend：发送消息
        // 参数1：Topic名称  参数2：消息内容
        rocketMQTemplate.convertAndSend(TOPIC_NAME, msg);

        return "消息发送成功";
    }
}
```

### 文件：`src/main/java/com/example/demo/listener/MqConsumerListener.java`（消费者）

```java
import org.apache.rocketmq.spring.annotation.RocketMQMessageListener;
import org.apache.rocketmq.spring.core.RocketMQListener;
import org.springframework.stereotype.Component;

@Component
// @RocketMQMessageListener：标记为 RocketMQ 消费者监听器
// topic → 监听哪个消息主题
// consumerGroup → 消费者所属分组（同组消费者集群模式分摊消息）
@RocketMQMessageListener(
        topic = MqProducerController.TOPIC_NAME,
        consumerGroup = "demo-consumer-group"
)
public class MqConsumerListener implements RocketMQListener<String> {

    /**
     * 收到消息时自动调用
     * @param message 消息内容，泛型 <String> 表示接收字符串类型消息
     */
    @Override
    public void onMessage(String message) {
        System.out.println("消费者收到消息：" + message);
        // 在这里写业务逻辑：发送短信、生成订单、数据同步...
    }
}
```

### RocketMQ 执行流程

```
① 启动 NameServer (端口 9876)
② 启动 Broker (端口 10911)，Broker 向 NameServer 注册
③ 启动 SpringBoot 项目
④ 访问 GET /sendMsg → 生产者发消息到 Topic
⑤ RocketMQTemplate → NameServer 获取 Broker 地址 → 消息发到 Broker
⑥ 消费者从 NameServer 获取 Broker 地址 → 拉取消息 → 执行 onMessage()
```

---

# 四、RabbitMQ vs RocketMQ 对比

| 特性 | RabbitMQ | RocketMQ |
|------|----------|----------|
| 开发组织 | Pivotal | 阿里开源 |
| 注册中心 | 无（直连 Broker） | NameServer |
| 核心概念 | Exchange + Queue + Binding | Topic + ConsumerGroup |
| SpringBoot starter | `spring-boot-starter-amqp` | `rocketmq-spring-boot-starter` |
| 发送 API | `RabbitTemplate.convertAndSend()` | `RocketMQTemplate.convertAndSend()` |
| 消费注解 | `@RabbitListener` | `@RocketMQMessageListener` |
| 消息模式 | 点对点、发布订阅 | 集群消费、广播消费 |
| 事务消息 | 支持较弱 | 原生支持 |
| 适用场景 | 业务通知、订单 | 电商、金融、事务消息 |

---

# 五、关键总结

## 5.1 邮件发送选型

| 场景 | 使用类 |
|------|--------|
| 简单纯文本通知 | `SimpleMailMessage` |
| HTML 富文本 | `MimeMessageHelper` + `helper.setText(html, true)` |
| 带附件 | `MimeMessageHelper` + `helper.addAttachment()` |
| 内嵌图片 | `MimeMessageHelper` + `helper.addInline()` |

## 5.2 消息队列选型

| 场景 | 推荐 |
|------|------|
| 业务通知、订单处理 | RabbitMQ |
| 电商、金融、事务消息 | RocketMQ |
| 日志收集、大数据流 | Kafka |

## 5.3 面试必备

| 知识点 | 答案 |
|--------|------|
| 消息队列三大作用 | **解耦、异步、削峰** |
| 邮件授权码是什么 | QQ邮箱开启 POP3/SMTP 后生成的专用密码，不是登录密码 |
| RocketMQ 启动顺序 | 先 NameServer → 再 Broker |
| NameServer 作用 | 路由注册中心，Broker 注册到 NameServer，生产者/消费者通过它发现 Broker |
| 集群消费 vs 广播消费 | 集群：同组分摊消息；广播：同组每个消费者都收到全量消息 |

## 5.4 文件速查表

| 章节 | 文件 | 说明 |
|------|------|------|
| 119 | `config/MailConfig.java` | 邮件配置（可选，大部分用 yml） |
| 119 | `service/MailService.java` | 简单邮件发送 |
| 120 | `service/MailService.java` | 多部件邮件（同上文件，新增方法） |
| 119-120 | `controller/MailController.java` | 邮件测试接口 |
| 121 | `config/RabbitConfig.java` | RabbitMQ 队列 Bean 注册 |
| 121 | `controller/MessageProducerController.java` | RabbitMQ 生产者 |
| 121 | `listener/MessageConsumer.java` | RabbitMQ 消费者 |
| 129 | `controller/MqProducerController.java` | RocketMQ 生产者 |
| 129 | `listener/MqConsumerListener.java` | RocketMQ 消费者 |
| 119-129 | `pom.xml` | 所有依赖汇总 |
| 119-129 | `resources/application.yml` | 邮件 + RabbitMQ + RocketMQ 配置汇总 |
