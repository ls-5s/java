# SpringBoot 运维实用篇

---

## 一、打包与运行

### 1.1 打包类型

| 类型 | 特点 | 适用场景 |
|------|------|----------|
| **jar（推荐）** | 内置Tomcat，服务器只装JDK即可运行 | 学习、中小型项目、主流部署 |
| **war** | 需外置Tomcat容器 | 老项目，现已少用 |

### 1.2 打包前置条件

pom.xml 必须引入 SpringBoot 父工程，自带打包插件：

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>2.7.15</version>
    <relativePath/>
</parent>
```

### 1.3 spring-boot-maven-plugin 打包插件

**插件作用：**
1. 将依赖、内置Tomcat、业务代码打包进一个可独立运行的 jar
2. 自动写入主启动类信息，保证 `java -jar` 正常启动
3. 执行 repackage 重打包，生成完整可部署包

**不配置该插件的后果：**
- jar 很小，缺少依赖，运行报 `ClassNotFoundException`
- 报 `jar中没有主清单属性`，无法用 `java -jar` 启动
- 无内置 Tomcat

**方式1：继承父工程（推荐）——无需写 version**

```xml
<build>
    <plugins>
        <plugin>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-maven-plugin</artifactId>
        </plugin>
    </plugins>
</build>
```

**方式2：未继承父工程——手动指定版本**

```xml
<plugin>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-maven-plugin</artifactId>
    <version>2.7.15</version>  <!-- 必须和SpringBoot版本一致 -->
</plugin>
```

**指定主启动类（多模块/插件无法自动识别时）：**

```xml
<plugin>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-maven-plugin</artifactId>
    <configuration>
        <mainClass>com.example.demo.DemoApplication</mainClass>
    </configuration>
</plugin>
```

### 1.4 打包方式

**方式1：IDEA 图形化**
1. 右侧 Maven 面板 → 展开项目 → Lifecycle
2. 双击 `clean` 清理旧文件
3. 双击 `package` 打包
4. 在 `target` 目录找到 `xxx.jar`

**方式2：命令行**

```bash
# 在 pom.xml 所在目录执行
mvn clean package -DskipTests
```

| 参数 | 作用 |
|------|------|
| `clean` | 清空旧打包文件 |
| `package` | 执行打包 |
| `-DskipTests` | 跳过单元测试，加快打包 |

### 1.5 运行命令

```bash
# 前台运行（窗口关闭即停止，仅本地测试）
java -jar demo.jar

# 后台常驻运行（Linux生产环境，断连不停止）
nohup java -jar demo.jar &

# 自定义日志文件
nohup java -jar demo.jar > app.log 2>&1 &

# 指定端口
java -jar demo.jar --server.port=8082

# 指定环境配置文件
java -jar demo.jar --spring.profiles.active=prod
```

### 1.6 Linux 停止服务

```bash
# 查询 Java 进程
ps -ef | grep java

# 强制关闭（替换为实际 PID）
kill -9 12345
```

### 1.7 完整部署流程

1. 上传 jar 包到服务器
2. 进入 jar 所在目录：`cd /home/project`
3. 后台启动：`nohup java -jar demo.jar &`
4. 查看日志确认：`tail -f nohup.out`
5. 停止时先查 PID 再 kill

### 1.8 常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 打包报错找不到主启动类 | 缺少打包插件 | pom.xml 添加 `spring-boot-maven-plugin` |
| `jar中没有主清单属性` | 未引入父工程+打包插件 | 补全父工程和插件配置 |
| 服务器访问不了接口 | 防火墙未放行 | 关闭防火墙/放行对应端口 |
| 关闭SSH窗口程序停止 | 使用了前台启动 | 改用 `nohup` 后台启动 |
| 端口冲突 | 默认端口被占用 | 启动时追加 `--server.port=自定义端口` |

---

## 二、启动临时属性

### 2.1 核心概念

在 `java -jar` 命令后追加 `--key=value`，即为临时属性。

- **优先级：** 命令行 > yml/application.properties 配置文件
- **生命周期：** 仅本次启动生效，重启后失效
- **适用场景：** 临时改端口、切换环境、临时改数据库参数，无需改代码/重新打包

### 2.2 语法规则

```bash
java -jar jar包名.jar --配置键=值
```

- 必须写在 jar 包名**后面**
- 多个参数空格隔开

### 2.3 常用命令

```bash
# 临时指定端口
java -jar demo.jar --server.port=8082

# 临时切换环境
java -jar demo.jar --spring.profiles.active=prod

# 同时传多个参数
nohup java -jar demo.jar --server.port=8083 --spring.profiles.active=test &

# 临时修改数据库连接
java -jar demo.jar --spring.datasource.url=jdbc:mysql://127.0.0.1:3306/testdb

# 本地开发环境 + 自定义端口
java -jar demo.jar --spring.profiles.active=dev --server.port=8081
```

### 2.4 临时属性 vs 永久配置

| | 临时属性 | 永久配置 |
|------|------|------|
| **方式** | 启动命令追加 `--参数` | 修改 application.yml |
| **是否需要打包** | 否 | 是 |
| **生效时长** | 仅本次 | 永久 |
| **适用场景** | 临时调试 | 长期部署 |

---

## 三、多环境配置

### 3.1 四级配置文件优先级（低 → 高）

```
application.yml  <  application-{env}.yml  <  jar同级外部yml  <  命令行临时参数
```

| 级别 | 文件/方式 | 说明 |
|------|-----------|------|
| 1 | `application.yml` | 公共通用配置，所有环境共享 |
| 2 | `application-{dev/test/prod}.yml` | 环境专属配置，覆盖同名 key |
| 3 | jar 包同级外部 yml | 不打包进 jar，线上可直改，无需重打包 |
| 4 | 命令行 `--key=value` | 优先级最高，仅本次生效 |

**覆盖示例：**

| 配置来源 | 端口值 |
|-----------|--------|
| application.yml | 8080 |
| application-dev.yml | 8081 |
| 外部同级 yml | 8084 |
| 命令行 `--server.port=8088` | **8088** ✅ 最终生效 |

### 3.2 多文件版配置

**文件结构（resources 下）：**

```
application.yml          # 公共配置
application-dev.yml      # 开发环境
application-test.yml     # 测试环境
application-prod.yml     # 生产环境
```

**application.yml（公共 + 指定默认环境）：**

```yaml
spring:
  application:
    name: demo-project
  profiles:
    active: dev   # 默认开发环境

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
```

**application-dev.yml（开发环境）：**

```yaml
server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/dev_db
    username: root
    password: 123456
logging:
  level:
    com.demo: debug
```

**application-test.yml（测试环境）：**

```yaml
server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://192.168.1.50:3306/test_db
    username: test
    password: Test666!
```

**application-prod.yml（生产环境）：**

```yaml
server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql://线上IP:3306/prod_db
    username: prod_user
    password: 线上加密密码
logging:
  level:
    root: error
```

**切换方式：**
- 永久：修改 `application.yml` 中 `spring.profiles.active`
- 临时：`java -jar demo.jar --spring.profiles.active=prod`

### 3.3 多环境分组管理

**作用：** 一次激活多个 profile，按功能拆分配置（数据库、Redis、OSS等），按需组合。

**application.yml（分组定义）：**

```yaml
spring:
  profiles:
    group:
      dev-group: db-dev, redis-dev
      prod-group: db-prod, redis-prod
    active: dev-group
```

**文件规划：**

```
application.yml              # 公共 + 分组定义
application-db-dev.yml       # 开发数据库
application-db-prod.yml      # 生产数据库
application-redis-dev.yml    # 开发 Redis
application-redis-prod.yml   # 生产 Redis
```

**启动命令：**

```bash
java -jar demo.jar --spring.profiles.active=prod-group
```

**与普通多环境的区别：**

| 普通多环境 | 分组管理 |
|-----------|---------|
| 一次只能激活一个 profile | 一次加载多套功能配置组合 |
| 所有配置耦合在一个文件 | 数据库、缓存等配置完全解耦 |

### 3.4 多环境开发控制（@Profile）

**作用：** 同一套代码，不同环境加载不同 Bean 和配置。

**配置块方式（yml）：**

```yaml
# 开发环境专属
---
spring:
  config:
    activate:
      on-profile: dev
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
knife4j:
  enable: true

# 生产环境专属
---
spring:
  config:
    activate:
      on-profile: prod
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
knife4j:
  enable: false
server:
  error:
    include-stacktrace: never
```

**注解方式（Java）：**

```java
// 仅 dev 环境生效
@Component
@Profile("dev")
public class DevDebugUtil { }

// 仅 prod 环境生效
@Component
@Profile("prod")
public class ProdSecurityInterceptor { }
```

**管控场景总结：**

| 环境 | 开启 | 关闭 |
|------|------|------|
| dev | 热部署、swagger、SQL打印、详细堆栈 | — |
| prod | 安全拦截器 | swagger、调试日志、详细堆栈 |

---

## 四、自定义配置文件

### 4.1 作用

存放自定义业务参数（文件路径、接口地址、密钥等），与系统配置分离。

### 4.2 自定义文件示例（custom.yml）

```yaml
upload:
  path: D:/upload/
  size: 102400
api:
  url: https://test.com
  key: abc123456
```

### 4.3 方式1：@Value 单个读取

```java
@Component
@PropertySource("classpath:custom.yml")
public class CustomConfig {
    @Value("${upload.path}")
    private String uploadPath;

    @Value("${api.key}")
    private String apiKey;
    // getter...
}
```

### 4.4 方式2：@ConfigurationProperties 批量绑定（推荐）

```java
@Component
@PropertySource("classpath:custom.yml")
@ConfigurationProperties(prefix = "upload")
public class UploadConfig {
    private String path;
    private Integer size;
    // 必须有 getter/setter
}
```

### 4.5 关键注意点

1. `@PropertySource` 只能读 classpath 内资源，服务器外部文件需用命令行或外部同级 yml
2. `@ConfigurationProperties` 类必须有 get/set 方法
3. 自定义文件优先级低于 application 系列配置

---

## 五、日志

### 5.1 日志框架与级别

SpringBoot 默认整合 **SLF4J + Logback**，无需额外导入依赖。

**日志级别（低 → 高）：**

```
trace  <  debug  <  info  <  warn  <  error
```

- 配置某级别后，只输出该级别及以上的日志
- 例：配置 `info` → 输出 info、warn、error，屏蔽 trace、debug
- 默认级别：`info`

### 5.2 快速创建日志对象

| 方式 | 代码 | 适用场景 |
|------|------|----------|
| **@Slf4j（推荐）** | 类上加 `@Slf4j`，直接用 `log.info()` | 企业常用，需 lombok |
| 传统写法 | `LoggerFactory.getLogger(Xxx.class)` | 不用 lombok |
| getClass 简化 | `LoggerFactory.getLogger(SimpleLogController.class)` | 无需 lombok，半简化 |

**@Slf4j 示例：**

```java
@Slf4j
@RestController
public class LogController {
    @GetMapping("/log")
    public void test() {
        log.debug("调试信息");
        log.info("正常业务");
        log.warn("警告");
        log.error("错误");
    }
}
```

### 5.3 日志输出格式控制

**常用占位符：**

| 占位符 | 作用 |
|--------|------|
| `%d{yyyy-MM-dd HH:mm:ss}` | 格式化日期时间 |
| `%thread` | 线程名 |
| `%-5level` | 日志级别（5字符对齐） |
| `%logger{36}` | 所属类（最长36字符） |
| `%msg` | 业务消息 |
| `%n` | 换行 |

**yml 配置（分环境）：**

```yaml
# 公共格式
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# 开发环境：彩色完整格式
---
spring:
  config:
    activate:
      on-profile: dev
logging:
  pattern:
    console: "%red(%d{yyyy-MM-dd HH:mm:ss}) [%yellow(%thread)] %highlight(%-5level) %green(%logger{36}) - %msg%n"
  level:
    root: debug

# 生产环境：精简格式
---
spring:
  config:
    activate:
      on-profile: prod
logging:
  pattern:
    file: "%d{HH:mm:ss} %-5level %logger{20} - %msg%n"
  level:
    root: info
  file:
    name: logs/demo.log
```

### 5.4 logback-spring.xml 完整配置（生产级）

**核心要素：** 按天分割、INFO/ERROR 分离存储、保留30天、超 5GB 自动清理。

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <!-- 定义日志存储根路径和输出格式 -->
    <property name="LOG_PATH" value="${user.home}/logs/gas-inspect"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"/>

    <!-- 1. 控制台输出（开发环境） -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 2. 业务日志文件：INFO ~ WARN（排除 ERROR） -->
    <appender name="FILE_INFO" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/info-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <!-- 排除 ERROR，避免和 error.log 重复 -->
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>DENY</onMatch>
            <onMismatch>ACCEPT</onMismatch>
        </filter>
    </appender>

    <!-- 3. 异常日志文件：仅 ERROR（方便运维快速定位） -->
    <appender name="FILE_ERROR" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/error-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- 全局根级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_INFO"/>
        <appender-ref ref="FILE_ERROR"/>
    </root>

    <!-- 单独设置指定包级别（如 Mapper 打印 SQL） -->
    <logger name="com.gas.inspect.mapper" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_INFO"/>
    </logger>
</configuration>
```

**彩色版 logback-spring.xml（开发用）：**

```xml
<configuration>
    <conversionRule conversionWord="clr" converterClass="ch.qos.logback.classic.pattern.ColorConverter"/>
    <conversionRule conversionWord="highlight" converterClass="ch.qos.logback.classic.pattern.HighlightConverter"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%clr(%d{yyyy-MM-dd HH:mm:ss}){blue} [%thread] %highlight(%-5level) %logger{36} : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="debug">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
```

### 5.5 线上运维命令

```bash
# 实时滚动查看日志
tail -f logs/demo.log

# 只看 ERROR 级别
grep "ERROR" logs/demo.log

# 查看最后100行
tail -n 100 logs/demo.log

# 搜索关键词
grep -i "关键词" logs/demo.log

# 清空日志文件（文件过大时）
> nohup.out

# 检索指定业务 ID 的日志
grep "workOrderId:10086" logs/demo.log

# 统计今日 ERROR 行数
grep -c "ERROR" logs/demo-2026-07-17.log

# 从 ERROR 日志中过滤 500 错误
grep "500" logs/error.log
```

### 5.6 多环境日志策略

| 配置项 | dev | prod |
|--------|-----|------|
| 日志级别 | debug | info |
| SQL打印 | 开启 | 关闭 |
| swagger | 开启 | 关闭 |
| 输出目标 | 控制台 | 控制台 + 文件滚动 |
| 格式 | 彩色完整 | 精简无彩色 |
| 归档策略 | 无需 | 按天分割、压缩、30天清理 |

**关键注意点：**
1. 颜色占位符仅控制台生效，日志文件不支持彩色（会乱码）
2. `%logger{数字}` 控制类名最大长度，防止日志过长
3. 优先级：`logback-spring.xml` > `application.yml`

### 5.7 避坑知识点

1. **配置文件名必须是 `logback-spring.xml`**，不能是 `logback.xml`，否则 SpringBoot 环境变量（如 `${user.home}`）无法识别
2. **Windows 路径用正斜杠**：写 `D:/logs/xxx`，不要用反斜杠 `\`
3. **`additivity="false"`**：避免日志重复打印到多个文件（子 logger 默认会向上传递到 root）
4. **生产环境不要开 `TRACE/DEBUG` 全局级别**，日志量过大会占满磁盘
5. **日志中禁止打印敏感信息**：手机号、身份证、密码等，合规要求

---

> **知识点索引**
> - 打包：1.1 ~ 1.8
> - 临时属性：2.1 ~ 2.4
> - 多环境配置：3.1 ~ 3.4
> - 自定义配置文件：4.1 ~ 4.5
> - 日志：5.1 ~ 5.7